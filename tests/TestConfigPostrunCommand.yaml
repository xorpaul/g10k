---
:cachedir: '/tmp/g10k'

sources:
  example:
    remote: 'https://github.com/xorpaul/g10k-test-environment.git'
    basedir: '/tmp/example/'
    prefix: true

postrun: ['tests/postrun.sh', '$modifiedenvs']
