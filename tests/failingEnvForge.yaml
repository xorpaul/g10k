---
:cachedir: '/tmp/g10k'
use_cache_fallback: false

deploy:
  purge_levels: [ 'deployment', 'environment', 'puppetfile' ]

sources:
  failforge:
    remote: 'https://github.com/xorpaul/g10k-test-env-invalid-forge.git'
    basedir: '/tmp/failforge/'
    prefix: true
    force_forge_versions: false
